{
  "name": "321-ch-server",
  "version": "1.0.0",
  "description": "",
  "main": "./server.ts",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "jest",
    "lint": "tslint -c ../tslint.json './**/*.ts*'",
    "build": "tsc",
    "compile": "tsc && node ./dist/server/src/server.js",
    "watch": "nodemon -e ts --exec \"npm run compile\"",
    "start": "node ./dist/server/src/server.js"
  },
  "author": "Oleg Stotsky",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.16.0",
    "@types/mongoose": "^5.0.16",
    "express": "^4.16.3",
    "mongoose": "^5.1.5"
  }
}
